# Applications

This directory contains deployable user-facing applications.

- `web/` — the primary browser-based workspace.
- `admin/` — optional dedicated administration experience, if it needs an independent deployment.
