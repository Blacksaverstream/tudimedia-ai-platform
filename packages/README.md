# Shared Packages

Place reusable, versioned code here. Typical packages include shared types, API clients, configuration, design-system components, and domain utilities. Packages must not depend on applications.
