# Services

This directory contains independently deployable backend services.

- `api/` — public and application API.
- `media-worker/` — upload validation, scanning, transcoding, and rendition generation.
- `ai-worker/` — asynchronous AI enrichment and evaluation jobs.
- `search/` — search indexing and retrieval concerns, when isolated from the API.
